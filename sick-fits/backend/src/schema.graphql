# Any queries you want access to need to go here
# What is the shape of the data?

# import * from './generated/prisma.graphql'

type Mutation {
    createItem(title: String, description: String, price: Int, 
    image: String, largeImage: String): Item!
}

type Query {
    items: [Item]!
}